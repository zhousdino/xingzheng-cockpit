# 2026-08-17 工作日志

## 资料库三表共享方案确认
- 用户确认采用「公开链接（获得链接的人可编辑）」方式开放 任务总表 / 车辆信息 / 签证情况 三张资料库表的填写权限。
- 复核 library skill：当前版本**无任何程序化共享/授权写接口**（api-manifest 仅 `space.permission.collaborators` 且标 `readonly`；apply/invite 能力关闭）。故共享必须在 WorkBuddy UI 手动完成（一次性）。
- 通过 `get_database_schema` 拉取三表真实结构（字段+下拉选项），用于生成同事填报须知，确保选项一字不差。

## 产出
- 新建 `填报须知.html`（沿用 cockpit 风格，单文件可分享）：含三表字段/类型/必填/下拉选项说明 + 公开链接安全提醒 + 整点同步说明。
- 待用户：在 WorkBuddy UI 为三表各开「公开可编辑」分享链接 → 把 3 个链接发回 → 我将其接入二级页「去资料库录入」按钮、把须知嵌入看板、重建并重新部署 CloudStudio。

## 签证表按用户业务重建（替换原 x2YEbPkkxTfnCNOxA1EWdM）
- 用户给的签证实际字段（截图）：姓名 / 护照类型 / 护照有效期至 / 签证类型 / 签证签发日期 / 签证到期日 / Iqama办理状态 / 最近一次飞签时间。
- library `add_database_field` 在当前环境**不生效**（返回 field_id 但 schema 不变）；delete 生效。故改用 `create_database` 整表重建。
- 新签证表 `database_id = 61g9FSYbQZreyiC1Praqai`（8 字段，顺序贴近截图）。旧表 x2YEbPkkxTfnCNOxA1EWdM 已弃用（不再被任何脚本引用）。
- 注意：资料库**无公式列**，故「护照剩余天数」不建列，改由驾驶舱前端按「护照有效期至」实时计算显示（临期≤30天/过期自动标红）。
- 已同步改：`lib_sync.py`（visa ID）、`bridge/parse.js`（mapVisaHeader/parseVisas 新字段）、`行政部驾驶舱_UI_v1.10.0.html`（parseVisas + renderVisas + 新增 pptRemainText + visaStatusClass 以 Iqama 为准）、`cockpit-style.css`（新增 .vtag.cancel）、`填报须知.html`（签证字段表）。
- 跑通 lib_sync（新表头正确写出）→ build.js → 重新部署 CloudStudio（链接不变）。线上当前 visas=0（空表待填）。

## 车辆表按完整版重建（替换原 IlIuElUzWtxAKPBMcGXpBg）
- 用户拍板车辆字段「推荐完整版（含租赁起止）」：补齐 年检到期/车架号/租赁起始日+到期日/车辆类别/燃料类型/使用单位/当前里程/下次保养日期，状态由 正常/保养/维修 扩为 +停用。
- 同样因 add_database_field 不生效，整表重建为新表 `database_id = ckVeiMCyKQ9qzTrK159dKk`（21 字段）。旧表 IlIuElUzWtxAKPBMcGXpBg 已弃用。
- 已同步改：`lib_sync.py`（vehicle ID）、`bridge/parse.js`（mapVehicleHeader/parseVehicles 新增 vehClass/fuel/vin/unit/leaseStart/leaseExp/inspExp/mileage/maintDate + 状态含停用 + 日期/权属归一化）、`行政部驾驶舱_UI_v1.10.0.html` 与 `车辆管理.html`（vehStatusClass 加 stop、vehCards 展示新字段、render KPI 增加 停用/临期过期预警、SAMPLE 加新字段）、`cockpit-style.css`（.vstatus.stop）、`填报须知.html`（车辆 21 字段表）、`车辆管理.html` 录入按钮换为新表链接。
- 跑通 lib_sync → build.js → 重新部署（链接不变）。线上当前 vehicles=0（空表待填，显示示例数据）。

## 会议室 20日会「没显示」诊断 + 修复
- 用户报：腾讯表序号12「2026.8.20 沙分第六次履约推进会（周舟）」在驾驶舱不显示。
- 排除：tdoc_call 读取/拉取正常（tencent_room.csv 含该条，parseRooms 也会保留它：normDate 把「2026.8.20」→「2026-08-20」，时间段拆出 start/end，过滤条件 `!start&&!booker&&!purpose` 不满足）。
- 真因：**部署数据陈旧**。上次 build.js→CloudStudio 部署发生在该会录入腾讯表之前，dist/index.html 内联数据缺这条（grep「履约推进会」=0）；而 12–17 日真实会已在（grep 杨明/HSE Induction=1），故看起来"只缺 20日"。
- 渲染规则（关键，防再误判）：会议室分两块——① 顶部时间轴卡片**只显示当天**（renderRooms 的 todayRooms 过滤 `r.date===today`）；② 底部「实时预约一览」列表显示 `!isRoomPast` 的全部（含未来，标「预定」）。所以未来的会**不在顶部时间轴，只在底部列表**。
- 修复：重跑 `static/build.js`（rooms=12，含 20日）→ 经 `workbuddy_cloudstudio_deploy` 重新部署 dist/，链接不变、verified=true，「履约推进会」已进 dist。
- 教训：录入新预约后，线上要等 hourly 部署自动化（automation-1786823878843）下一次跑才会刷新；要立刻显示需手动 build.js + 部署。

## v1.15.0 · 车辆/签证看板「批量导入模板 + 文件导入」+ 新增签证管理看板
- 用户要求：车辆管理页、签证管理页都增加「文件导入接口」+「表单模板下载链接」，以便一次性批量导入数据。
- 在 车辆管理.html / 新建 签证管理.html 加入通用批量导入模块（内联 JS，自包含、无后端）：
  - 「⬇ 下载导入模板」：生成与资料库字段**一字不差**的 CSV（含 BOM，Excel 中文不乱码）+ 3 行空白。
  - 「⬆ 导入数据文件」：选 CSV → 本地解析（支持逗号/制表符、引号、GBK/UTF-8 自动识别）→ 逐行校验（必填缺失/日期格式/下拉项）→ 滚动预览表（通过/注意/错误三色）+ 汇总。
  - 「导出核对文件」：把校验后数据存 CSV，交管理员后台真实写入资料库（静态页无法直连资料库，出于安全）。
  - 下拉项取自 `get_database_schema` 真实表结构（车辆 4 个单选 + 签证 3 个单选），避免导入时选项不匹配。
- 下拉选项（权威，来自 schema）：
  - 车辆：车辆类别=公务轿车/SUV越野/皮卡/中巴客车/货车；燃料类型=汽油/柴油/新能源；所有情况=自购/租赁；状态=正常/保养/维修/停用。
  - 签证：护照类型=普通护照/公务护照/外交护照；签证类型=工作签证/商务签证/访问签证/旅游签证/其他；Iqama办理状态=未开始/办理中/已完成/已注销。
- 新建 `签证管理.html`（只读看板，镜像车辆页：KPI 6 卡 + 签证台账表 + 去资料库录入按钮指向 61g9FSYbQZreyiC1Praqai + 风格切换 + 导入模块）。
- 主页 `行政部驾驶舱_UI_v1.10.0.html` `topnav` 加「签证管理看板」链接；`填报须知.html` 加「⑤ 批量导入」小节（模板下载→填→校验→导出核对文件→管理员后台写入）。
- `static/build.js` 的 SECONDARY 列表加入 `签证管理.html`；build.js 重跑 → `workbuddy_cloudstudio_deploy` 重新部署 dist/，链接不变、verified=true。
- 新增 `lib_import.py`（CSV→资料库，batch_add 追加模式，含 schema 下拉校验、日期/数字归一、≤100 分批、dry-run、导出核对 JSON），作为管理员后台真实写入工具。
- **⚠️ 资料库平台级读取 bug（待向 WorkBuddy 反馈）**：经 `batch_add`/`add` 写入的记录**能落库（get_record 按 id 可取）但不在列表读取（get_database_content / query 返回 0 行）**；仅 `import_csv` 创建的记录可被列表读到，但 import_csv 是覆盖式且会破坏 select 选项。结论：本环境 API/manual 写入不进列表索引 → 驾驶舱读取链路（lib_sync→parse→build 内联）对"新增数据"暂时读不到，看板仍回退示例数据。已告知用户并选「先反馈 bug + 前端照做」。待官方修复后，dashboard 才能显示真实录入数据。
- 清理：删除本次联调的临时探针文件（_test_*.csv / _probe*.csv / _*.json / _dbg.txt）；保留 lib_import.py。测试用探针资料库（_probe_db / _probe2 / import_csv 探针）为空表，未删（无删库 API，需 UI 手动）。
