# 2026-08-16 工作日志

- 在金山表格《行政部驾驶舱模板_v1.1.0.xlsx》(file_id Rug8vwy4zxMVRwY7Ed56rxPrsqLuHwqka) 实际新建并就位两个车辆 sheet 表头，闭环 v1.10.0 的「车辆情况」模块：
  - 「车辆信息」(sheetId 5，新建)：车牌 / 车型 / 保险到期 / 驾驶员 / 通行证 / 通行证到期 / 状态 / 所有情况 / 当前位置 / 上报时间 / 备注（列名与 bridge/parse.js 的 mapVehicleHeader 模糊识别对齐）。
  - 「车辆排期」(sheetId 4，文件里此前已存在空表，直接复用写表头)：日期 / 车牌 / 任务 / 驾驶人 / 出发 / 目的地 / 开始 / 结束 / 备注（与 mapVehSchedHeader 对齐）。
  - kdocs 连接器写文本值的限制：update_range_data / add_row / range_data_batch_update 的 op_type 枚举仅 formula/format/merge/picture，**无纯文本写值**；写表头用 opType=formula 写 ="xxx" 字面量公式，get_range_data 返回的 originalCellValue 是纯文本，wps_flatten.py 优先取 originalCellValue，解析正常。
  - 已手动跑通完整同步链路：kdocs get_range_data → wps_flatten.py 拍平写 drop/vehicle/vehicle_info.csv 与 drop/vehicle_sched/vehicle_sched.csv → node static/build.js → 重新部署 CloudStudio（同一链接）。线上车辆板块现为真实空态（待填写）。自动化 automation-1786823878843 步骤 2.5/2.6 已覆盖这两 sheet 的每小时读取。
  - 用户下一步：在金山表两 sheet 填入真实车辆信息，下一个整点自动化自动同步进驾驶舱 ② 车辆情况。

- UI 板块顺序调整（用户指令"车辆放在会议室之后"）：将「车辆情况」从原第四位上移到会议室之后，主板块顺序变为 **① 会议室实时预约 → ② 车辆情况 → ③ 签证情况 → ④ 部门整体任务**。改动：模板 DOM 重排 + 编号重标（②③④④→②③④④重编）+ 页脚顺序说明同步；bridge/server.js、static/build.js 引用不受文件名影响（保持 v1.10.0）。重建静态版并重新部署 CloudStudio（同一链接不变）。属布局微调，未升版本号（CHANGELOG 可记一笔）。

- v1.11.0 UI 三项修订（用户当晚三条指令）：① 会议室显示部门——腾讯表本就有「部门」列，给 bridge/parse.js 与模板内联 mapRoomHeader 加 `dept` 映射，`renderRooms` 在每间卡片列表项与"实时预约一览"项显示部门（蓝徽章）；② 车辆首页看板精简——`vehCards` 重构为只显示「当前位置 + 今日安排」，保险/驾驶员/通行证等具体信息收进 `<details class="vdetail">` 折叠（点开查看），头部保留状态/自购租赁小标签；③ 移动端自适应——新增 `detectMobile()`（UA+屏宽给 `<html>` 加 `ui-mobile` 类，随 resize 同步）+ `@media(max-width:600px)` 与 `html.ui-mobile` 紧凑布局（单栏、表格横向滚动、顶栏堆叠、车辆卡片单列）。无头渲染测试通过（部门 安监部 显示、车辆折叠详情生成）。重建静态版、重部署 CloudStudio（同链接）、重启 localhost:8787 桥接（带 dept）。文件名沿用 v1.10.0。

- v1.12.0 分角色视角（不同的人看不同内容）——用户问"能否开启不同的人看不同的内容、不同的人对不同的板块可以修改"，结论：**修改继续走原表格（金山/腾讯自带共享权限），看不同内容用前端软身份过滤**。已落地实现（纯前端，不动后端/表格/自动化）：
  - 顶部新增"当前视角"下拉（总览 + 自动从任务表「负责人/协同人」收集的人名 + 手写补充），首次打开弹"引导选人"层，localStorage 记住选择。
  - 确认的设计决策：**签证全员可见但非管理员隐藏姓名**（遮成 ＊＊，管理员可见真实姓名）；**车辆全员可见**；**默认引导选人**进个人视图；**人名自动收集+手写补充**（CONFIG.admins 标管理员，如 老周）。
  - 实现要点：CONFIG/STATE + isAdmin()/currentViewTasks()/buildRoster()/showGuide()/pickViewer()；render() 改用过滤后的 VIEW（逾期标记抽到 markOverdue）；部门任务板块加"查看范围：只看我的/看全部"；会议室加"只看与我相关的预约"开关；签证姓名按 isAdmin 遮罩。
  - 验证：Node 抽取真实函数单测通过（管理员看全部3条、小王非管理员只看自己负责/协同的2条、切看全部3条、会议室只看我2条、名册收集含管理员）；node --check 语法通过；node static/build.js 重建成功（会议室10条真实数据）。文件名沿用 v1.10.0；CHANGELOG 记 v1.12.0。
  - 待用户：在 CONFIG.admins 填实际管理员姓名（现为示例 ['老周']）；在金山/腾讯表配各板块"谁可编辑"的共享权限（硬控制修改）。

- **回退到 v1.10.0（2026-08-16 当晚，用户决定）**：用户说"算了，版本回溯v1.10.0"，并明确选**彻底回退**（连 v1.11.0 一并撤销）。操作：以 `git checkout 7aa296b` 还原 `行政部驾驶舱_UI_v1.10.0.html` / `bridge/parse.js` / `static` 静态产物（7aa296b = 完整 v1.10.0，含"车辆移至会议室之后"那次调整，页脚"布局优先级版 v1.10.0"），删除 `分角色视图方案_v1.12.0.md`，`node static/build.js` 重建（rooms=10 真实数据；任务/签证/车辆仍为空，待填）。本次为**新增提交还原**（不改写历史，正常 push）。**当前代码 = 完整 v1.10.0**；v1.11.0（移动端自适应/车辆看板精简/会议室部门显示）与 v1.12.0（分角色视角）改动仅保留在 git 历史，可随时 cherry-pick 恢复。随后重新部署 CloudStudio 使线上链接同步为 v1.10.0。

- **v1.11.0 新增「车辆/任务表单录入页」+ 风格统一（2026-08-16 深夜，本轮）**：用户提出三条新需求，一个一个做：① 单独的车辆信息配置入口与车辆管理 UI（表单化，不直接改金山表）；② 同理单独的任务填写入口与页面 UI；③ 所有二级 UI 与主页风格一致（主页改风格二级页跟随）。已交付：
  - 抽出 `cockpit-style.css` 为**单一风格来源**（5 套皮肤变量 + 通用组件类），主页 `<style>` 改为 `<link rel="stylesheet" href="cockpit-style.css">`，bridge/server.js 静态路由新增 `.css/.html` 从项目根目录提供，static/build.js 内联该 CSS 进静态产物（Req3 落地）。
  - 新增 `车辆管理.html`（Req1）：表单录入车辆台账，提交 `POST /api/upload {type:'vehicles'}` → 落 `bridge/drop/vehicle/uploaded_*.csv`；KPI + 车辆卡片 + 新增/编辑弹层；与主页共用皮肤与 `.tag` 状态标签。
  - 新增 `任务管理.html`（Req2）：表单录入部门任务（字段对齐金山「任务总表」表头：任务名称/所属大类/子项备注/负责人/协同人/状态/进度%/开始日期/截止日期/优先级/堵点说明），提交 `POST /api/upload {type:'tasks'}` → 落 `bridge/drop/wps/uploaded_*.csv`；KPI + 状态/分类筛选 + 看板卡片。
  - 两页 `loadData()` 优先 `window.__COCKPIT_DATA__`（静态构建注入）→ `/api/data`（本地桥接）→ 示例数据；static/build.js 现已一并把两页注入内嵌数据 + 内联 CSS 打包进 `dist/` 与 `static/`，部署后线上也能显示真实数据、风格一致。
  - **修 bridge/server.js 坑**：静态路由 `new URL().pathname` 在 Node 下返回**百分号编码的中文文件名**，导致 `fs.existsSync` 找不到真实文件、二级页面 404；已对 basename 做 `decodeURIComponent` 修复。
  - **修 bridge/parse.js mapHeader 坑（同时影响主页真实数据显示）**：`堵点说明` 误命中 NOTE 规则（`/备注|子项|说明/`）排在 BLOCKER 之前，导致 note/blocker 错位（堵点内容进 note、blocker 空）；已调整为先匹配 `/堵点|卡点|阻碍/` 并收紧 NOTE 为 `/备注|子项/`。
  - 提交 `3f418ff`（v1.11.0）已 push master；CloudStudio 重新部署（同一链接 `e8645aaa…app.workbuddy.link`，dist→static 部署成功），线上三页均验证 200 + 真实内容。
  - **遗留待决（重要）**：表单提交的**写回金山**尚未接通。当前流程：表单 → `/api/upload` → `drop/*/uploaded_*.csv` → 仅本地桥接 `/api/data` 能看到；**线上静态页无服务器收 POST**，且没有任何自动化把 `uploaded_*.csv` 追加写回金山 sheet。所以：本地桥接模式下用户自己填能本地看到，但不会回写金山；线上/部门其他人通过公开链接填则 POST 直接失败（"未连接本地桥接服务"）。需与用户确认线上填报方案：A) 部门其他人继续用金山共享编辑链接（最简单）；B) 部署一个在线接收端（需额外托管/服务）；C) 加一个自动化：读 `drop/*/uploaded_*.csv` 经 kdocs 连接器追加到金山对应 sheet（仅对「用户本机跑桥接、自己填」有效，对纯线上公开链接无效，因上传落不到 drop）。

- **v1.13.0 架构迁移完成（2026-08-16，本轮收尾）**：把任务/车辆/签证三表后台从「金山(WPS)在线表格 + kdocs 连接器」整体迁移到 **WorkBuddy 内置「资料库」云数据表**，并**废弃 bridge 桥接服务**。
  - 已在资料库建 3 张 database：任务总表 `E810IQGqpV9dHEwsnuIsb7`、车辆信息 `IlIuElUzWtxAKPBMcGXpBg`、签证情况 `x2YEbPkkxTfnCNOxA1EWdM`（字段名与 bridge/parse.js 表头映射对齐，零改造同步）。
  - 新增 `lib_sync.py`：每小时经资料库 API 拉三表 CSV 落 `bridge/drop/{wps,vehicle,visa}`（替代原 kdocs + wps_flatten.py 链路）；腾讯会议室仍由 automation-1786812677644 经连接器写入 `bridge/drop/tencent`。
  - 主页 `行政部驾驶舱_UI_v1.10.0.html`：新增「快捷入口」导航（`topnav`/`navbtn`，复用 cockpit-style.css 变量）跳转到两个二级看板；**移除旧的「手动同步入口(WPS/腾讯粘贴上传)」折叠区及桥接相关 JS**（按钮绑定置空会报错，已一并清理）；同步状态改为"每小时自动刷新（资料库 + 腾讯会议室）"；修正板块②车辆/③签证 sec-sub 里"WPS/金山文档连接器"措辞为「资料库云数据表」。
  - 二级页面 `车辆管理.html` / `任务管理.html` 由"表单提交录入"改为**只读看板 + 去资料库录入按钮**（直达对应资料库云表共享填报），移除表单与 `POST /api/upload` 逻辑。
  - `cockpit-style.css` 新增 `.topnav`/`.navbtn` 风格类（与 `.tbtn` 同变量）；`static/build.js` 复用不变（仍读取 bridge/drop 下 CSV），重建 `dist/`/`static/`。
  - 自动化 `automation-1786823878843`（HOURLY）改为：connect_open_platform(library) 取 token → `lib_sync.py` → `node static/build.js` → CloudStudio 重新部署（同一链接小时级刷新）；原 WPS 任务表/签证表两个 kdocs 自动化已删除。
  - 已重建并重新部署 CloudStudio（链接不变：https://26e39b5a0aed44d7b0bfbb45dd2fd2c6.app.workbuddy.link）。
  - **待用户（重要）**：在 WorkBuddy 内把 3 张资料库云表「共享/授权编辑」给部门成员（当前 skill 无程序化共享 API，需一次性 UI 操作），部门其他人才能在资料库填报，驾驶舱才会显示真实数据。另：3 张表目前为空，线上先显示示例数据兜底。

- **v1.15.1 hotfix · 修复"返回驾驶舱"链接失效（2026-08-16）**：用户截图反馈二级页面（车辆/签证/任务）的"返回驾驶舱"链接点不动。根因：项目根目录无 `index.html`（仅 `static/build.js` 生成到 `dist/`、`static/`），本地 `present_files` 预览 / 双击打开时相对链接（`index.html` / `./`）解析到根目录下不存在的 index.html → 404；线上部署站因 index.html 与二级页同目录反而正常。修复：三张二级页（车辆管理.html / 签证管理.html / 任务管理.html）返回链接改为绝对部署 URL `https://26e39b5a0aed44d7b0bfbb45dd2fd2c6.app.workbuddy.link`，三种场景（本地预览 / 部署站 / file://）均指向真实主页。重跑 build.js 重打包 → CloudStudio 重新部署（链接不变，verified=true）。教训：静态二级页的跨页链接用绝对部署 URL 最稳，勿依赖根目录 index.html 存在。
