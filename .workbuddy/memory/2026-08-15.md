# 2026-08-15

- 需求沟通：部门多人驾驶舱，平台定为**飞书多维表格**，约 12 人（名单未定，需支持增减/改分工），六大类（会务接待/车辆管理/后勤管理/物资采购/行政办公/人力资源），进度用数字+进度条。
- 产出《行政驾驶舱 搭建蓝图与操作手册 v1.0.0.html》+ CHANGELOG.md（含飞书分步操作手册、GitHub/PAT 获取说明、版本编号原则）。
- 本地 git 初始化并提交、打 tag `v1.0.0`；草案文件已删除。
- 验证本环境可连 GitHub（curl 返回 200）。
- GitHub 已建仓库 zhousdino/xingzheng-cockpit，v1.0.0 已 push 带 tag。

## 飞书建表方案变更（关键约束）
- 用户飞书环境：表在"个人云盘"，「移动到」只有 我的文件夹/共享文件夹，**无团队空间/知识库入口**，无法把自建应用加为协作者 → API 脚本方案(91403 Forbidden)走不通。
- 改走**手动搭建**：已出《行政驾驶舱_手动搭建清单_v1.0.0.html》，含 13 字段(含是否逾期公式/更新时间)、4 视图、仪表盘、逾期标红条件格式、自动化。
- 备用路甲：若用户能获团队空间或网页版知识库把表移入并加应用协作者，仍可改用 build_feishu_cockpit.py 脚本自动建。
- 凭证状态：用户已发飞书 app_id/app_secret/app_token + GitHub token（明文在对话，已提醒用后重置/吊销）。
- 用户提供凭证并运行：GitHub 仓库 zhousdino/xingzheng-cockpit 已建，v1.0.0 已推送（HTML+CHANGELOG+tag）。
- 飞书脚本连通正常（tenant token OK、默认字段改名成功），但建字段/视图报 91403 Forbidden = 自建应用尚未被加为表的协作者。日历视图 API 不支持，改手动加。待用户把应用加为协作者后重跑脚本。
- 用户云盘里发现「共享工作台」文件夹 + 「经营分析」共享多维表，判断存在共享空间。用户选择把「行政部驾驶舱」移入「共享工作台」，再试应用协作者授权。
- **结论：API 自动建表方案彻底放弃。** 飞书「移动到」只能选"我的文档库/共享文件夹(空)"，"全部知识库/我的文件夹"点不开，环境无任何可写团队/企业空间 → 自建应用无法被加为协作者，脚本 91403 无解。最终 100% 走**手动搭建**（清单已就绪）。
- **用户放弃飞书，改换 WPS 在线表格**（2026-08-15 约 11:08 决定）。原因：飞书环境限制过多、反复折腾。WPS 为在线Excel型，无看板/人员字段，改用 下拉+数据条+筛选+图表 实现驾驶舱。
- 已出《行政驾驶舱_手动搭建清单_WPS版_v1.1.0.html》+ 更新 CHANGELOG(v1.1.0)、MEMORY.md(平台改WPS)。飞书脚本 build_feishu_cockpit.py / check_fields.py 作废。
- 注意：验证飞书旧 token 时已发现旧多维表被删(note has been deleted)，用户可能新建了表但 app_token 失效；飞书凭证已无用。GitHub token 仍有效(仓库已建)。

## WPS 模板交付与导入（约 11:30）
- 因无法登录用户 WPS 账号操作，改为**生成 Excel 模板**(行政部驾驶舱模板_v1.1.0.xlsx) 让用户导入：含 13 列、3 下拉(大类/状态/优先级)、逾期整行标红($L2="逾期"→红)、状态着色(已完成绿/进行中黄)、进度%数据条、逾期公式、仪表盘(统计卡+状态饼图+大类柱状图)。
- 用户已**导入 WPS 在线表格**(链接 cpWfRalYY8qT，旧空表)，导入完成。
- 读回 xlsx 逐条核对结构全部正确（条件格式公式存于 rule.formula，非 formula1，确认无误）。
- 后续告知用户：分享同事(可编辑)、筛选D列看每人、WPS 不支持"按行限制可见"的细粒度权限(飞书才有)。
- 平台最终定 WPS。飞书/腾讯文档凭证已作废提示用户重置。

## 独立驾驶舱 UI v1.2.0（约 14:15）
- 用户要求：驾驶舱 UI 需"另外设计"，与 WPS 后台表分离。
- 交付 `行政部驾驶舱_UI_v1.2.0.html`：单文件纯前端、离线可用、零外部依赖(图表用原生SVG/CSS，不引第三方库)。
- 板块：KPI卡(总/进行中/已完成/逾期/完成率环形)、状态分布、大类任务数、优先级分布、人员工作台(每人任务数+平均进度+逾期)、逾期清单、全部任务明细(进度条)。
- 数据接入(免授权)：WPS 全选复制→粘贴框→生成；或上传CSV。按表头名自动识别列。
- 内置18条示例数据(12占位同事)，打开即有展示；"恢复示例"还原。
- 视觉：中建蓝#2E75B6主色+党建红#C2272D预警，浅色专业风，克制不花哨。
- 已用 node+DOM桩测试：解析/逾期判定/CSV/各板块渲染均通过。
- 已提交+打tag v1.2.0+push GitHub(与 v1.0.0/v1.1.0 共存)。

## 风格切换器 v1.3.0（约 14:30）
- 用户嫌 v1.2.0 浅色不好看，要求先看多种风格再选；先出《行政部驾驶舱_风格选择预览.html》内置 5 风格(深色科技大屏/浅色商务/毛玻璃渐变/政务简约/暖橙现代)可切换对比。
- 用户最终："都保留吧，可以更换" → 把 5 套皮肤做成正式版内置切换器。
- 交付 `行政部驾驶舱_UI_v1.3.0.html`：**在 v1.2.0 全部功能基础上内置 5 套可切换皮肤**，顶部「界面风格」按钮一键换肤；选择用 localStorage 记住(下次沿用)。皮肤用 CSS 变量，所有图表/进度环随主题刷新。默认深色科技大屏。
- 原预览页冗余，已删除；v1.3.0 取代之。
- 已 node+DOM桩测试：主题切换/记忆/KPI/donut(var变量)/人员/逾期/大类渲染均通过。
- 已提交+打tag v1.3.0+push GitHub(四 tag 全在)。
- 现状：WPS 后台表(用户已导入) + 本独立 UI(用户查看大屏) 双轨；用户尚未填真实数据，仍用示例占位。

## 会议室实时预约模块 v1.4.0（约 14:40）
- 用户新增需求：已有**腾讯在线表格**记录会议室预约，要求驾驶舱**同步显示会议室实时预约/使用情况**，归属"会务接待"板块；明确"其他内容读 WPS，会议室读腾讯"。
- 设计：**双数据源分离**——任务数据继续读 WPS（原粘贴/CSV 入口）；新增专属"会议室数据"入口读腾讯表格（独立粘贴框/CSV/可选公开链接同步）。
- 会议室表头自动识别：会议室/房间、日期、开始/结束时间、预约人、用途、状态；支持"起止时间"合并列(09:00-10:30)自动拆分。
- **实时判定**：基于当前时间算每间状态(使用中/空闲/即将开始≤30min)与每条预约状态(使用中/即将开始/待开始/已结束)，页面每 60 秒自动刷新 + "实时刷新中"脉冲 + 当前时间显示。
- 板块：会议室总数/使用中/空闲/即将开始 统计卡 + 每间"今日时间轴"(7:00–22:00 横向排程+当前时间竖线) + 今日预约明细(带实时状态标签)；随 5 套皮肤换色。
- 限制说明：纯静态 HTML 无法直连私有腾讯表格，主路径仍是"从腾讯表复制粘贴/上传CSV"；"公开链接同步"为 fetch 最佳努力(跨域常受限，失败回退提示)。已向用户讲清。
- 已 node+DOM桩测试：会议室解析(7)、实时状态/KPI、时间轴seg/nowmark、CSV、主题切换、任务解析(18)与逾期判定均通过（过程曾因测试脚本漏拼表头误报，已更正确认非产品bug）。
- 已提交+打tag v1.4.0+push GitHub(五 tag 全在)。

## 本地桥接服务 v1.5.0（约 15:45）——解决"实时更新 WPS+腾讯表"
- 用户诉求："我需要实时更新 WPS 和腾讯的表格"。即驾驶舱要自动拉最新数据，而非每次手动粘贴。
- 经 AskUserQuestion 确认为：**只读同步 + 本地桥接**（最稳、数据不出本机）。
- 根因：WPS/腾讯表均为「登录可见」私有表，纯静态网页读不到（v1.4.1 已实测腾讯表 401 + 浏览器 CORS）。方案：在你本机跑 Node 服务用一次性授权去拉数据存本地，网页自动读。
- 交付 **bridge/** 目录（新增）：`server.js`(纯 Node 零依赖：托管网页+`/api/data`+`/api/sync`+`/api/upload`同源上传+监听`drop/`目录自动读CSV)、`parse.js`(与前端一致的任务/会议室解析)、`fetchers.js`(WPS官方OpenAPI/公开链接 + 腾讯公开链接 best-effort，失败回退)、`config.example.json`/`config.json`(含腾讯预填链接+空token，gitignore)、`.gitignore`(屏蔽 config.json/data/drop)、`README.md`。
- 三种喂数据方式（按稳当排序）：① **网页粘贴框点"上传到本地服务"**(推荐，同源无跨域)；② **丢 CSV 到 drop/wps 或 drop/tencent**(常开即自动读)；③ 填公开链接让服务端抓(最佳努力，私有会失败回退)。
- 进阶自动（可选非必需）：WPS 用官方 OpenAPI 填 fileToken+access_token；或两表都设「公开可读」填 publicLink（央企慎用以免数据暴露公网）。
- 驾驶舱 UI v1.5.0 改动：顶部"自动同步"状态条(显示最近更新时间+各来源模式/错误)；任务/会议室粘贴框新增"上传到本地服务"按钮；"同步读取"改走本地服务端(绕 CORS)；启动即拉 `/api/data` 覆盖示例，每 60 秒轮询。
- 经单壳端到端测试：`/api/data`、`/api/upload`(TSV+CSV)、`/api/sync`(含 drop 回退)、初始空状态均通过。
- 已提交+打tag v1.5.0+push GitHub(六 tag 全在)；origin 分支为 master（非 main），远程已加 PAT。
- 使用提示（给用户）：在 bridge 目录 `node server.js`，浏览器开 `http://localhost:8787`；关窗即停。不是云端服务，只在用户本机跑。
- **安全重申**：对话中明文 GitHub token(ghp_...) 与旧飞书 app_secret 仍在，务必用后重置/吊销（bridge 不依赖这些旧凭证）。

## v1.6.0 · 本地桥接「全自动同步」(16:22 起)
- 用户要求 WPS + 腾讯 都"真自动"跑通（非手动）。
- 架构：本地 Node 服务(bridge/server.js) 每 60s 自动抓。WPS 支持 ①官方 OpenAPI(accessToken+fileToken,最稳) ②浏览器Cookie走 drive.kdocs.cn 下载接口。腾讯仅能 浏览器Cookie → docs.qq.com export_office 导出 → 轮询 → 下xlsx。
- bridge/xlsx-read.js：零依赖 xlsx 解析（仅 zlib 内置），解决无 npm 安装权限问题；修复数字实体 &#20219; 解码。
- 驾驶舱升 v1.6.0：顶部状态条分「任务(WPS)」「会议室(腾讯)」显示各自同步方式+报错。
- 已推 GitHub + tag v1.6.0。
- 留待用户：填 Cookie/Token 到 bridge/config.json（WPS OpenAPI 需 developer.kdocs.cn 建应用）。

## v1.6.1 · 腾讯表经「腾讯文档连接器」直读成功（17:23 起，关键突破）
- 用户发来腾讯表深链（tdoc://300000000_PSWNplWZFbfx，标题「会议室预约登记表」）。先让用户连上 WorkBuddy 的腾讯文档连接器 → tdoc_init 返回 READY。
- 经 tencent-docs 技能 `tdoc_call sheet-mcp get_cell_data`（file_url=docs.qq.com/sheet/DUFNXTnBsV1pGYmZ4?tab=BB08J2, sheet_id=BB08J2, return_csv=true）**成功直读私有表，无需 Cookie**！
- 真实表结构（与之前假设不同）：首行标题「会议室预约登记表」、表头=序号/预约日期/时间段/预约人/联系电话/参会人数/会议主题；**无"会议室"列、无"状态"列**；日期形如 2026.8.12；时间"时间段"合并起止且常含全角冒号 12：00。
- 修正 bridge/parse.js 与驾驶舱 parseRoom：①跳过标题行 ②无会议室列→默认归"会议室"分组 ③日期归一化 ④全角冒号归一化 ⑤"时间段"正确拆 start/end ⑥新增参会人数/联系电话。验证真实 6 条预约正确流入 /api/data。
- 驾驶舱升 v1.6.1（修解析=小版本）；server.js 指向 v1.6.1；已提交+tag v1.6.1+push GitHub。
- **新建自动化「会议室表(腾讯)定时同步到驾驶舱」(automation-1786805823789, HOURLY)**：每小时经连接器直读该表→写 bridge/drop/tencent/tencent_room.csv（桥接自动读）+ 顺带 POST /api/upload 即时刷新。只读，不改原表。
- 结论：腾讯侧"真自动"已用连接器打通（不再依赖 Cookie）。WPS 侧仍需本机桥接(OpenAPI/Cookie)。桥接服务须在本机运行，自动化写入的 CSV 才会被读到。

## v1.6.2 · 修复腾讯表「双语表头含换行」导致解析为 0 条（17:5x 起）
- 现象：连接器实读的 csv_data 表头是「预约日期\nDATE」这种双语写法，引号内带真实换行；原 parseCSV 把引号内换行也当行分隔 → 表头行被拆散 → findHeaderLine 找不到≥2识别列 → 会议室解析为空(0 条)。
- 修复 bridge/parse.js 与 UI 内嵌 parseCSV：引号内 \n/\r 改为空格(保留整行)。实测解析出 6 条有效预约（含全角冒号 12：00-13：30 已归一到 12:00/13:30）。
- 经连接器实读 + 写 drop/tencent/tencent_room.csv + 本地 node parseRooms 验证 6 条通过；server.js 启动读 drop 验证 /api/data 返回 rooms=6。
- 驾驶舱升 v1.6.2（修解析=小版本）；server.js 指向 v1.6.2；已提交+tag v1.6.2+push GitHub。
- 注意：server.js 启动先 loadState 旧 data.json，若旧 state 已有 rooms 则跳过 drop 重读（mode 标签可能停留在 tencent-none）；全新机器(空 data.json)首次启动会走 drop 读并标 drop-file。不影响数据正确性。

## v1.6.3 · WPS 任务表也经「金山文档(kdocs)连接器」直读成功（18:3x 起）
- 用户本会话连上 WPS 连接器（金山文档 / kdocs，状态 connected）。
- 实测经 `mcp__kdocs__sheet.get_range_data`（file_id=Rug8vwy4zxMVRwY7Ed56rxPrsqLuHwqka，worksheet_id=1，range 0..400×0..13）**免 Cookie/免 OpenAPI 直读私有表《行政部驾驶舱模板_v1.1.0.xlsx》「任务总表」**成功（表头 13 列 + 逾期公式均读到）。
- 该表经 search_files 搜「驾驶舱」定位到（用户云盘里它叫"行政部驾驶舱模板_v1.1.0.xlsx"，不是"任务总表"命名）。
- **关键发现：这张表目前是空模板**——只有表头 + 第11列逾期公式(拖到60行)，没有任何任务行。所以连接器读通了但表里没数据可显示。
- 新增 bridge/wps_flatten.py：把 kdocs 返回的稀疏单元格 JSON（data.detail.rangeData，含 rowFrom/colFrom/originalCellValue）拍平为标准 CSV，供桥接端 parseTasks 复用（与腾讯路径对称）。
- 新增自动化「WPS任务表(金山文档)定时同步到驾驶舱」(automation-1786808634457, HOURLY, ACTIVE)：每小时经连接器直读→写 drop/wps/wps_task.csv（桥接每60s重读自动刷新）。只读，不改原表。
- 验证：合成 rangeData→wps_flatten→CSV→parseTasks 出 2 条正确（含 deadline/start 日期归一化）；真实桥接端 drop-file 路径 tasks=2 验证通过。**后已清掉模拟 CSV、复位 data.json，呈现真实状态：tasks=0(空模板)、rooms=6(腾讯真实)**。
- 顺手：parseTasks 对「开始日期」也做日期归一化（与截止日期一致），UI 内嵌解析逻辑同步修正。
- 驾驶舱升 v1.6.3；server.js 指向 v1.6.3；已提交+tag v1.6.3+push GitHub。
- **现状**：腾讯(会议室)与 WPS(任务)两侧均已"真自动"经连接器打通。WPS 表是空模板，待用户往里填任务即自动显示。

## v1.6.4 · 修取数：直接打开 HTML 也能拉到实时数据（18:5x 起）
- 用户反馈"会议室的数据你也没更新呀"。排查：后端数据其实是新的（自动化每小时同步 + 我手动强制同步，当前 8 条会议室，含用户新增的 8-17 杨凤利 11:00 行），`/api/data` 返回正常。
- 根因：页面用**相对路径** `fetch('/api/data')`，只有经桥接服务 `http://localhost:8787` 打开时才指向正确接口；直接打开静态 HTML 文件（或预览面板加载文件本体）时该请求落到错误地址 → 只看得到示例版式，误以为"数据没更新"。
- 修复：loadData 改为依次尝试 `/api/data` 与 `http://localhost:8787/api/data`，拉到任一即显示真实数据，都失败才退回示例并提示"未连接本地桥接服务"。升 v1.6.4（修显示/取数=修订），server.js 指向 v1.6.4。
- 新增 bridge/force_sync_rooms.py：手动强制重读腾讯表并 POST 推桥接（补漏每小时自动化之间的间隙，用户"刚加了预约想立刻看"时用）。
- 已提交+tag v1.6.4+push GitHub。
- **重要环境约束（需牢记）**：桥接服务(bridge/server.js)运行在 WorkBuddy 沙箱内，不在用户本机；用户浏览器能否直连 localhost:8787 取决于沙箱端口转发。最稳的"看实时数据"方式是：让助手 present `http://localhost:8787`（预览面板与沙箱同网，能加载实时页）。若用户在自己机器跑 `node bridge/server.js`，则他们本机的 localhost:8787 才是数据源。

## v1.7.0 · 会议室"全部预约(按日期)"视图 + 逐条状态徽章(已开过/今天/预定)（19:1x 起）
- 用户要求：核对腾讯表每条日期与"今天"是否一致，判断预定还是已开过，**别瞎写**。
- 实测核对（基准 2026-08-15，北京时间约 22:04）：腾讯表《会议室预约登记表》共 9 行。序号3（8-14）仅有日期无预约内容（空占位）。其余 8 条有效：已开过 6 条（8-12 杨明、8-13 Intakhabun Nasir、8-15 上午 Arshad S(HSE)、8-15 中午 余成、8-15 晚 杨凤利）；预定 2 条（8-16 杨凤利 周例会、8-17 靳程 设计周例会、8-17 杨凤利 周例会——注 8-17 有 2 条）。
- 原 renderRooms 只显示"今天"的会议，看不到过去/未来，且无线日期状态标注 → 用户分不清历史与未来。
- 新增 roomDateStatus() + 会议室区底部"全部预约"列表（按日期+时间排序，逐条【已开过(灰)/今天(蓝)/预定(绿)】徽章）。
- **关键修复**：roomDateStatus 初版用 `new Date(y,m,d,h,mi)` 构造，受运行环境时区影响（沙箱沙特 GMT+3 把北京时间误当沙特时间，导致 8-15 20:00 误判为今天）。改为**纯数字比较**（todayStr / nowMin / timeToMin，均为本地），彻底规避时区偏差；在用户本机(北京)天然正确。已在 node 用显式北京 now=22:11 验证全 9 行状态正确。
- 驾驶舱升 v1.7.0（新增视图模块=次版本）；server.js 指向 v1.7.0；已提交+tag v1.7.0+push GitHub。
- 根 .gitignore 补 `.workbuddy/automations/` 忽略（自动化运行时数据不入库）。

## v1.7.1 · 收起手动同步区为折叠备用入口（19:1x 起）
- 用户截图反馈：顶部「任务数据 / 会议室数据」的粘贴框、立即同步、恢复示例等按钮在自动同步已接通时不需要一直显示。
- 改动：把任务和会议室两个 databar + 展开粘贴区合并成一个默认折叠的 `<details>`「手动同步入口（备用，自动同步已接通时无需打开）」；顶部状态条简化为「数据状态」一行，已连接显示「自动同步正常 · 任务：… · 会议室：…」，未连接提示访问 `http://localhost:8787`。
- 驾驶舱升 v1.7.1（显示优化=修订）；server.js 指向 v1.7.1；已提交+tag v1.7.1+push GitHub。
- 当前状态：桥接服务(v1.7.1)正在沙箱跑，`http://localhost:8787` 可访问；真实数据 tasks=0（WPS空模板）、rooms=8（腾讯）。

## 重新核查"实时更新"——发现两个同步自动化已丢失并重建（19:5x 起）
- 用户要求重新检查两个在线表格实时更新是否正常。核查发现：automation 列表里**只剩 1 个**(利雅得价格调研)，之前建的两个驾驶舱同步自动化（腾讯 automation-1786805823789、WPS automation-1786808634457）**已被删除(not found)**。即"每小时自动同步"当前并未在跑，CSV 是早前手动同步遗留（WPS 19:40 / 腾讯 18:57）。
- **重建两个 HOURLY 自动化**：腾讯会议室→automation-1786812677644；WPS任务→automation-1786812677546（均已 ACTIVE，cwds 指向项目目录）。MEMORY.md 旧 ID 已同步更新。
- **实测连接器链路仍正常**：tdoc_init 返回 READY；`tdoc_call sheet-mcp get_cell_data`(file_url+BB08J2+return_csv) 直读腾讯表 78 行 CSV 落盘；kdocs `get_range_data`(file_id+worksheet_id=1+range) 拉到 21 万字符稀疏 JSON → wps_flatten.py 拍平 402 行 CSV 落盘。两侧均验证可读、解析正常。
- **腾讯表当前 9 行**：8 条有效预约（2026.08.12–08.17），8-14 为空占位；WPS 任务表仍是空模板（402 行，待填）。
- 沙箱桥接已重启（路径坑"落地的的做法"多一个"的"又踩一次，已修正为"落地的做法"），`/api/data` 返回 rooms=8 / tasks=0，根路径返回 v1.7.1 主页。
- 结论：实时更新能力已恢复（自动化重建+链路验证通过）；桥接在沙箱跑，present `http://localhost:8787` 预览面板可看实时页。

