# 行政部驾驶舱 · 分角色视图（不同的人看不同内容）设计方案

> 版本：v1.12.0（规划稿）
> 日期：2026-08-16
> 结论前置：本方案只改"看"，不改"改"。修改仍走原表格（金山/腾讯自带共享权限）。

---

## 一、目标

让同一份驾驶舱，不同的人打开看到的内容不一样：

1. **普通同事**进来，主要看"我负责 / 我协同"的任务，以及工作安排类公开板块；敏感板块（如签证）对他隐藏。
2. **领导 / 行政主管**进来，看全部 + 领导视图（隐藏细节），并能看到签证等敏感板块。
3. 谁都不用跳来跳去——顶部选一下"我是谁"即可切换视角。

---

## 二、总体思路（一句话）

**软身份 + 前端过滤，不动后端、不动表格、不动现有自动化。**

- 驾驶舱现在的数据是内嵌进 HTML 的（方案 B），每张表里本来就有「负责人」「协同人」两列，过滤条件现成。
- 顶部加一个"我是谁"的下拉，选了之后用前端 JS 把不属于这个人的内容藏掉 / 筛掉。
- 因为是静态页，身份是"软"的（谁都能在顶部切到别人名字）。对行政部这种内部互信小团队完全够用；**真登录需要后端，本方案不做**。（修改权限的"硬控制"交给原表格的共享权限，见第七节。）

---

## 三、身份（人员名册）从哪来

两种结合，最稳：

1. **自动收集**：从任务总表的「负责人」+「协同人」两列去重，自动生成可选人名。
2. **手写补充**：在 HTML 顶部一处 `CONFIG.roster` 里，手动补几个暂时没任务但在册的人（比如领导、行政主管、新来的）。

下拉选项 = 自动收集 ∪ 手写补充，再加一个默认的「全部 / 总览」。

---

## 四、角色与板块可见性矩阵

| 板块 | 普通同事（选了具体人） | 管理员（领导 / 行政主管） |
|------|----------------------|--------------------------|
| ① 会议室实时预约 | 可见（可开"只看与我相关"） | 可见（全部） |
| ② 车辆情况 | 可见 | 可见 |
| ③ 签证情况 | **隐藏** | 可见 |
| ④ 部门任务 | 默认进入"我的任务"筛选 | 全部 + 领导视图 |
| 领导视图（隐藏细节） | **隐藏** | 可见 |

> 说明：会议室、车辆属于"工作安排"，建议全员可见；签证涉及个人信息，默认只给管理员。这几条都可以按你要求再调。

---

## 五、"我的任务"过滤逻辑

选了具体人（比如"老周"）后，部门任务板块按这条规则筛：

```
任务保留，当且仅当：
  任务的「负责人」== 当前人
  或 任务的「协同人」包含当前人
```

看板（按负责人 / 按状态）、日历、领导视图都套同一过滤。顶部留一个"看全部 / 只看我的"开关，方便临时切换。

会议室额外加一个"只看与我相关"——只显示预约人 = 当前人的预约。

---

## 六、顶部身份栏交互

- 位置：顶栏右侧，一个下拉「我是：全部 ▾ / 老周 / 小李 / …」。
- 选「全部」= 回到总览（等同于管理员视角）。
- 选具体人 = 进入"个人视图"（按第四节矩阵显隐 + 第五节过滤）。
- 用 localStorage 记住上次选择，下次打开沿用。
- 移动端：身份栏在顶栏换行或收进一个"菜单"按钮，不挤。

---

## 七、"修改不同板块"怎么落地（不改代码，配表格权限）

编辑继续在金山 / 腾讯文档里做，用它们自带的"共享与权限"：

- **任务总表（金山）**：把表格共享给部门所有人，权限设为"可编辑"；每人只改自己负责的行（靠表结构 + 分工纪律）。若要更细，可改用金山「多维表格」的"字段权限"（指定某人只能看/改某几列）。
- **签证 sheet（金山）**：权限设为仅 HR / 分管领导"可编辑"，其余人"仅查看"或"不可见"。这是敏感板块的硬控制点。
- **会议室表（腾讯）**：按需要把预约人加为协作者，谁预约谁填。
- **车辆信息 / 车辆排期（金山）**：行政相关人"可编辑"，其余"仅查看"。

> 我会另附一份《表格共享权限配置清单》，一步步写清楚每个表点哪里设。

---

## 八、与现有架构的兼容性

- 不影响方案 B 内嵌数据、CloudStudio 在线链接、桥接服务、每小时自动化。
- 只改前端渲染逻辑（加身份状态 + 过滤 + 显隐），数据字段不动。
- 重新 `node static/build.js` 打包静态版并重部署即可，同链接不变。

---

## 九、我的实施步骤（确认后执行）

1. HTML 顶部加 `CONFIG.roster`（手写补充名单）+ 自动收集逻辑。
2. 加顶部身份栏 UI + 对应 CSS（含移动端适配）。
3. 加 `currentUser` 状态 + localStorage 记忆。
4. 改渲染：按第四节矩阵显隐板块；任务类视图加"我的任务"过滤 + "看全部/只看我的"开关。
5. 会议室加"只看与我相关"筛选。
6. 重建静态版 → 重部署 CloudStudio（同链接）→ 重启本地桥接验证。
7. 更新 CHANGELOG / MEMORY / 当日日志。

---

## 十、已确认的设计决策（2026-08-16）

1. **签证板块**：全员可见，但**隐藏姓名**（普通同事看不到谁办的签证；管理员可见姓名）。
2. **车辆板块**：**全员可见**。
3. **默认进入**：**引导选人**——首次打开弹出选身份，默认进"个人视图"；记住上次选择。
4. **人员名册**：**自动收集 + 手写补充结合**。手写补充里可标记"管理员"（领导/行政主管），管理员可见姓名 + 领导视图。

> 由此细化：需新增 `CONFIG.admins`（手写管理员名单，如 老周/行政主管）。非管理员 = 个人视图（我的任务 + 会议室可开"只看与我相关" + 车辆全看 + 签证看进度但遮姓名 + 无领导视图）；管理员 = 总览视角（全部 + 姓名 + 领导视图）。

---

## 十一、签证"隐藏姓名"的渲染规则

- 每条签证卡片的「姓名」列：非管理员视图显示为 `某某` / `*` 遮罩（保留行数，只看进度）；管理员视图显示真实姓名。
- 顶部留"看全部/只看我的"不适用签证；签证对个人视图只遮姓名，不排除任何人（因为全员可见）。
