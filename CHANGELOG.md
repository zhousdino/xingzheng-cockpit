## v1.15.0 · 2026-08-17
**车辆/签证看板新增「批量导入模板 + 文件导入」；新增签证管理看板**
- 车辆管理.html / 新建 签证管理.html 各内联一套自包含批量导入模块（无后端、不直连资料库）：
  - 「⬇ 下载导入模板」生成与资料库字段一字不差的 CSV（含 BOM，Excel 中文不乱码）+ 3 行空白。
  - 「⬆ 导入数据文件」本地解析（逗号/制表符、引号、GBK/UTF-8 自动识别）→ 逐行校验（必填/日期/下拉项）→ 滚动预览（通过/注意/错误三色）+ 汇总。
  - 「导出核对文件」存 CSV，交管理员后台真实写入资料库（静态页出于安全无法直连）。
  - 下拉项取自 `get_database_schema` 真实表结构（车辆 4 单选 / 签证 3 单选），避免导入选项不匹配。
- 新建 `签证管理.html` 只读看板（镜像车辆页：KPI 6 卡 + 签证台账表 + 去资料库录入按钮指向 `61g9FSYbQZreyiC1Praqai` + 风格切换 + 导入模块）。
- 主页 `topnav` 加「签证管理看板」链接；`填报须知.html` 加「⑤ 批量导入」小节（模板下载→填→校验→导出核对文件→管理员后台写入）。
- `static/build.js` 的 SECONDARY 列表加入 `签证管理.html`；重跑 build.js → CloudStudio 重新部署 dist/（链接不变，verified=true）。
- 新增 `lib_import.py`（CSV→资料库，batch_add 追加，schema 下拉校验 + 日期/数字归一 + ≤100 分批 + dry-run），作为管理员后台真实写入工具。
- 清理联调临时探针文件；保留 lib_import.py。
- **已知 ⚠️ 资料库读取 bug**：API/manual 写入的记录暂不被列表读取（get_database_content/query 返回 0 行），仅 import_csv 创建的记录可读（但会破坏 select 选项）。当前环境 dashboard 对"新增数据"读不到、回退示例数据，待官方修复。

## v1.14.0 · 2026-08-17
**车辆表完整版重建（21 字段，含租赁起止 + 年检 + 车架号等缺口）**
- 资料库「车辆信息」整表重建为新表 `ckVeiMCyKQ9qzTrK159dKk`（旧表 `IlIuElUzWtxAKPBMcGXpBg` 弃用；因 library `add_database_field` 在当前环境不生效，沿用 create_database 整表重建）。
- 字段由 12 扩为 21：新增 车辆类别(单选)/燃料类型(单选)/车架号/使用单位/租赁起始日/租赁到期日/年检到期/当前里程(数字)/下次保养日期；状态由 正常/保养/维修 扩为 +停用（满足用户"租赁需知租赁期限"要求）。
- 同步改：`lib_sync.py`(vehicle ID)、`bridge/parse.js`(mapVehicleHeader/parseVehicles 新字段+状态含停用+日期/权属归一化)、`行政部驾驶舱_UI_v1.10.0.html` 与 `车辆管理.html`(vehStatusClass 加 stop、vehCards 展示新车架号/类别/燃料/使用单位/年检/租赁/里程/保养、render KPI 增 停用/临期过期预警、SAMPLE 加新字段)、`cockpit-style.css`(.vstatus.stop)、`填报须知.html`(车辆 21 字段表)、`车辆管理.html` 录入按钮换为新表链接。
- 重新 build + 部署 CloudStudio（链接不变）。

## v1.13.0 · 2026-08-16
**架构迁移：资料库云数据表替代金山/WPS，废弃本地桥接服务**
- 数据源切换：任务/车辆/签证 三表后台由「金山(WPS)在线表格 + kdocs 连接器」改为 **WorkBuddy 内置「资料库」云数据表**（3 张 database：任务总表 / 车辆信息 / 签证情况），字段名与 bridge/parse.js 表头映射对齐，零改造同步。会议室仍走腾讯在线表格 + 腾讯文档连接器（不变）。
- 新增 `lib_sync.py`：每小时经资料库 API 拉三表 CSV 落 `bridge/drop/{wps,vehicle,visa}`（替代原 kdocs 连接器 + wps_flatten.py 链路）。
- **废弃 bridge 桥接服务**：移除主页「手动同步入口（WPS/腾讯粘贴上传）」折叠区及相关 JS（按钮绑定置空会报错，已一并清理）；主页同步状态改为"每小时自动刷新（资料库 + 腾讯会议室）"；不再有 `/api/upload`、`/api/data` 实时链路。
- 二级页面（`车辆管理.html` / `任务管理.html`）由"表单提交录入"改为**只读看板 + 去资料库录入按钮**（直达对应资料库云表共享填报），移除表单 / `POST /api/upload` 逻辑。
- 主页新增「快捷入口」导航（`topnav`/`navbtn`，复用 `cockpit-style.css` 变量），一键跳转两二级看板；二级页保留"返回驾驶舱"链接，风格与主页统一（单一来源 cockpit-style.css）。
- 自动化 `automation-1786823878843` 改为：connect_open_platform(library) 取 token → `lib_sync.py` → `node static/build.js` → CloudStudio 重新部署（同一链接小时级刷新）；原 WPS 任务表/签证表两个 kdocs 自动化已删除，腾讯会议室自动化保留。
- 重建并重新部署 CloudStudio（链接不变）。部门多人填报需用户在 WorkBuddy 把 3 张资料库云表「共享/授权编辑」给成员（当前 skill 无程序化共享 API）。

## v1.10.0 · 2026-08-15
**会议室隐藏已开过的会议；新增车辆情况模块（④）；方案B 静态版同步车辆数据**
- 会议室（①）：首页不再显示已开过的会议——过滤掉「过去日期 + 今日已结束」的场次，仅保留**使用中**与**预约（待开始/未来）**；同步修掉表头"部门"列被误映射到预约人的既有解析 bug。
- 车辆情况（④，用户要求回归）：首页新增「车辆情况」入口/板块，点入为车辆详情视图。每车一张卡片，承载：车牌 / 保险（含到期，临期·过期高亮）/ 驾驶员 / 通行证 / 车辆状态（正常·保养·维修）/ 车辆所有情况（自购·租赁）+ 当日排期 + 当前位置。
  - 数据来自金山(WPS)表格**新建的两个 sheet**：「车辆信息」（静态台账）与「车辆排期」（当日安排）。两 sheet 由用户手动在《行政部驾驶舱模板_v1.1.0.xlsx》中新建（当前尚不存在 → 显示示例数据预览 + 空态引导）。
  - 「当前位置」采用 **A 方案（计划位置）**：取该车当日排期的「目的地」作为当前/预计位置，零额外录入；代码同时**兼容 B 方案**——若「车辆信息」sheet 增加了"当前位置+上报时间"两列，页面自动优先显示真实上报位置，无需改代码（C 二维码签到为后续可选升级）。
  - 新建示例车牌（新A·12345 / 新B·67890 / 新C·11223）用于未建 sheet 时的预览。
- bridge/parse.js：新增 parseVehicles（车辆信息）+ parseVehicleSched（车辆排期），表头名正则模糊识别，均加入 module.exports。
- bridge/server.js：STATE 增加 vehicles / vehiclesched 与 sources 对应项；doSync 读取 drop/vehicle、drop/vehiclesched；/api/upload 支持 type='vehicles'/'vehiclesched'；**修复 loadState 整体覆盖导致新增字段丢失**（改为合并保留，避免回退丢 vehicles）；UI_FILE 指向 v1.10.0。
- static/build.js：读取车辆两表并内嵌到 `window.__COCKPIT_DATA__.vehicles/vehiclesched`；模板 loadData 内嵌分支同步处理，向后兼容桥接。
- 自动化「行政部驾驶舱_静态版每小时生成与部署」(automation-1786823878843) 更新：新增步骤 2.5 读取「车辆信息」sheet → drop/vehicle/vehicle_info.csv、2.6 读取「车辆排期」sheet → drop/vehicle_sched/vehicle_sched.csv（找不到即跳过，只读不改原表）。
- 文件重命名：`行政部驾驶舱_UI_v1.9.0.html` → `行政部驾驶舱_UI_v1.10.0.html`（git mv 保留历史）；build.js、server.js 的 UI_FILE/TEMPLATE 同步更新。

## v1.10.0 修订 · 2026-08-16
- UI 板块顺序调整（用户指令"车辆放在会议室之后"）：将「车辆情况」从原第四位上移到会议室之后，主板块顺序改为 **① 会议室实时预约 → ② 车辆情况 → ③ 签证情况 → ④ 部门整体任务**（原 ②③④④ 重编号；页脚顺序说明同步）。模板 DOM 重排 + 编号重标，bridge/server.js、static/build.js 引用不受影响（保持 v1.10.0 文件名）。

## v1.9.0 · 2026-08-15
**按"会议室 → 签证 → 部门任务"重排 UI，新增签证情况模块；移除车辆安排模块**
- 用户要求：第一直观看会议室，第二看签证情况，第三看部门整体任务。"车辆先不管了"。据此重排：
  - ① 会议室实时预约（置顶，不变）；
  - ② 签证情况（新模块，数据来自 WPS 在线表格「签证」sheet）；
  - ③ 部门整体任务（不变）。
- 新增 renderVisas() + parseVisas()（bridge/parse.js 同步新增）：从 WPS「签证」sheet 读取，按表头名模糊识别（姓名/国家/签证类型/状态/提交日期/预计出签/有效期至/备注）。
- 签证 KPI：办理人数 / 办理中 / 已出签 / 临期·过期；表格含状态徽章，并基于"有效期至"与今天对比自动标红（已过期=红、临期≤30天=黄）。
- bridge：STATE 增加 visas 字段与 sources.visas；doSync 读取 drop/visa（由新自动化写入）；/api/upload 支持 type='visas'；UI_FILE 指向 v1.9.0。
- 新增自动化「签证表(金山文档)定时同步到驾驶舱」(automation-1786815070791, HOURLY, ACTIVE)：经 kdocs 连接器 get_sheets_info 按名称定位「签证」sheet → get_range_data → wps_flatten.py → 写 bridge/drop/visa/visa.csv（只读不改原表）。
- 手动同步入口折叠区内新增「签证情况」粘贴框 + 「上传到本地服务」+ CSV 上传。
- 说明：用户将在金山表格新增「签证」sheet，每小时自动同步后 ②签证 即显示真实数据；当前为空态引导。

## v1.8.0 · 2026-08-15
**按"会议室 → 车辆 → 部门任务"优先级重排 UI，新增车辆安排模块**
- 用户要求：首先直观看见会议室情况，其次车辆安排，再次部门整体任务。据此重排三大板块顺序：
  - ① 会议室实时预约（置顶 hero，最直观：每间时间轴 + 今日预约 + 全部预约按 已开过/今天/预定 标注）；
  - ② 车辆安排（新模块，集中展示 WPS「车辆管理」大类的车辆事项，含车辆 KPI + 事项表 + 空态引导）；
  - ③ 部门整体任务（总体情况：KPI + 完成率 + 状态分布 + 各大类 + 优先级 + 人员工作台 + 逾期 + 全部任务）。
- 新增 renderVehicles()：从任务总表筛选 category==='车辆管理' 渲染；空态提示「请在 WPS 表的车辆管理大类下填写车辆事项（年检 / 维修 / 调度 / 油料等）」。
- 说明：当前 WPS 任务表为空模板，②车辆 与 ③部门任务 暂显示空态 / 待填报；会议室(①) 有真实数据（腾讯表 8 条）。填了即自动显示。
- 升 v1.8.0，bridge/server.js 的 UI_FILE 指向 v1.8.0。

## v1.7.1 · 2026-08-15
**简化顶部手动同步区：收进折叠备用入口，自动同步时页面更干净**
- 用户反馈：WPS 和腾讯都已自动同步，页面上方「任务数据 / 会议室数据」的粘贴框、立即同步、恢复示例等按钮不再需要一直显示。
- 把任务和会议室两个 databar + 展开粘贴区合并成一个默认折叠的 `<details>`，summary 为「手动同步入口（备用，自动同步已接通时无需打开）」。
- 顶部状态条简化为「数据状态」一行；已连接时显示「自动同步正常 · 任务：… · 会议室：…」；未连接时明确提示「请访问 http://localhost:8787 查看实时数据；或展开下方手动同步入口粘贴数据」。
- 升 v1.7.1，server.js 指向 v1.7.1。

## v1.7.0 · 2026-08-15
**会议室新增「全部预约（按日期）」视图 + 逐条状态徽章（已开过 / 今天 / 预定）**
- 用户要求核对腾讯表每条日期与"今天"的关系，判断是"预定"还是"已开过"，不要瞎写。
- 实测核对（基准日 2026-08-15）：腾讯表《会议室预约登记表》共 9 行，序号3（8-14）仅有日期无预约内容；其余 8 条有效中 —— 已开过 6 条（8-12 杨明、8-13 Intakhabun、8-15 上午 Arshad / 中午 余成 / 晚 杨凤利），预定 2 条（8-16 杨凤利、8-17 靳程 + 8-17 杨凤利）。
- 修复：原 renderRooms 只显示"今天"的会议，看不到过去/未来，且无线日期状态标注。新增 roomDateStatus() 按**浏览器本机时间**判断（避免沙箱沙特时区误判），并在会议室区底部加"全部预约"列表，逐条带【已开过(灰)/今天(蓝)/预定(绿)】徽章，按日期+时间排序。
- 升 v1.7.0，server.js 指向 v1.7.0。

## v1.6.4 · 2026-08-15
**修取数：直接打开 HTML 文件也能拉到实时数据**
- 根因：页面用相对路径 `fetch('/api/data')`，只有经桥接服务 `http://localhost:8787` 打开时才指向正确接口；直接打开静态 HTML 文件（或预览面板加载文件本体）时该请求落到错误地址 → 只看得到示例版式，误以为"数据没更新"。
- 修复：loadData 改为依次尝试 `/api/data` 与 `http://localhost:8787/api/data`，拉到任一即显示真实数据，都失败才退回示例并提示"未连接本地桥接服务"。
- 实测：后端会议室数据已由每小时自动化 + 手动强制同步保证最新（当前 8 条，含用户新增的 8-17 杨凤利 11:00 行）；本修复后无论以何种方式打开页面都能看到实时会议室。
- 升 v1.6.4，server.js 指向 v1.6.4。

## v1.6.3 · 2026-08-15
**WPS 任务表也接入「连接器真自动」同步（与腾讯同款）**
- 用户在本会话连上 WPS 连接器（金山文档 / kdocs）。实测经 mcp__kdocs__sheet.get_range_data 直读私有表《行政部驾驶舱模板_v1.1.0.xlsx》「任务总表」成功——无需 Cookie、无需 OpenAPI token。
- 新增 bridge/wps_flatten.py：把 kdocs 返回的稀疏单元格 JSON（data.detail.rangeData，含 rowFrom/colFrom/originalCellValue）拍平为标准 CSV，供桥接端 parseTasks 复用。
- 新增自动化 `automation-1786808634457`（HOURLY，ACTIVE）：每小时读该 WPS 表 → 写 drop/wps/wps_task.csv → 桥接服务（每 60s 重读）自动刷新驾驶舱。
- 注意：当前这张表是**空模板**（仅表头 + 第 11 列逾期公式，无任务行）。自动化已打通管道，待用户往里填任务即自动显示。
- 顺手：parseTasks 对「开始日期」也做日期归一化（与截止日期一致）；UI 内嵌解析逻辑同步修正。
- 已提交 + 打 tag v1.6.3 + 推送 GitHub。

## v1.6.2 · 2026-08-15
**修复：腾讯表真实数据「双语表头含换行」导致解析失败（0 条）**
- 根因：腾讯表表头单元格为「预约日期\nDATE」这类双语写法，API 返回的 csv_data 在引号内带真实换行；原 parseCSV 把引号内换行也当行分隔，导致表头行被拆散、findHeaderLine 找不到 ≥2 个识别列 → 会议室解析为空。
- 修复 bridge/parse.js 与 UI 内嵌 parseCSV：引号内遇到 \n/\r 改为空格（保留整行），表头不再被拆散；实测真实 csv_data 现稳定解析出 6 条有效预约。
- 顺手：server.js 的 UI_FILE 指向 v1.6.2，页面未找到提示文件名同步更新。
- 经 connector 实读 + 本地 parseRooms 验证：6 条会议室（含全角冒号时间 12：00-13：30 已归一为 12:00/13:30）均正确流入。
- 已提交 + 打 tag v1.6.2 + 推送 GitHub。

## v1.6.1 · 2026-08-15
**修正腾讯《会议室预约登记表》真实列映射（数据贯通验证通过）**
- bridge/parse.js：适配真实表结构——首行为标题“会议室预约登记表”（自动跳过）、表头为 序号/预约日期/时间段/预约人/联系电话/参会人数/会议主题；无“会议室”列、无“状态”列。
- 修复：1) 无会议室列时默认归入“会议室”分组（不再整行丢弃）；2) 日期 2026.8.12 归一化为 2026-08-12；3) 时间全角冒号 12：00 归一化为 12:00；4) “时间段”合并起止正确拆分为 start/end；5) 新增 参会人数 / 联系电话 字段。
- 驾驶舱 UI 同步修正 parseRoom + 渲染（列表项显示参会人数）。
- 经 WorkBuddy 腾讯文档连接器实读该私有表成功（无需 Cookie），真实 6 条预约已验证流入 /api/data。
- 已提交 + 打 tag v1.6.1 + 推送 GitHub。


## v1.6.0 · 2026-08-15
**本地桥接服务升级为「全自动同步」**
- bridge/xlsx-read.js：新增零依赖 xlsx 解析（仅用 Node 内置 zlib，无需 npm install），可直接解析 WPS/腾讯导出的 xlsx。
- bridge/fetchers.js 重写：
  - WPS 支持两条全自动路：① 官方 OpenAPI（accessToken+fileToken，最稳长期）；② 浏览器 Cookie 走内部下载接口。
  - 腾讯支持：浏览器 Cookie 调内部 export_office 导出 → 轮询进度 → 下载 xlsx → 解析（私有表唯一真自动路）。
- 驾驶舱 UI 升 v1.6.0：顶部状态条分别显示「任务(WPS)」与「会议室(腾讯)」各自用了什么同步方式及失败原因，未配置凭据时给出引导。
- README 重写：把「全自动」从可选进阶提升为核心能力，写明 WPS OpenAPI/Cookie 与腾讯 Cookie 的获取步骤。
- 仍保留「浏览器上传」「丢 CSV」作为稳当兜底。
- 已提交 + 打 tag v1.6.0 + 推送 GitHub。

# CHANGELOG · 行政部驾驶舱

## v1.5.0 · 2026-08-15
- **新增：本地桥接服务（bridge/），让驾驶舱"实时更新 WPS + 腾讯表格"成为现实。**
- 核心矛盾解决：WPS/腾讯表均为「登录可见」私有表，纯静态网页读不到（此前 v1.4.1 已实测腾讯表 401 + 浏览器 CORS 拦截）。本版用"在你本机跑一个小服务 + 你的一次性授权"绕过这道墙：数据不出本机、不暴露公网。
- 服务职责（纯 Node 零依赖 `server.js`）：① 托管驾驶舱网页；② `GET /api/data` 返回最新数据；③ `POST /api/sync` 触发重新抓取；④ `POST /api/upload` 浏览器同源上传（最稳、无跨域）；⑤ 监听 `drop/` 文件夹自动读入丢入的 CSV。
- 三种喂数据方式（按稳当程度）：① 网页粘贴框点"上传到本地服务"（推荐，同源无跨域）；② 把 CSV 丢进 `drop/wps/` 或 `drop/tencent/`（常开即自动读）；③ 在 `config.json` 填公开链接让服务端抓（best-effort，私有表仍会失败并回退）。
- 进阶自动配置（可选，非必需）：WPS 用官方 OpenAPI（填 fileToken + access_token）；或两表都设「公开可读」填 publicLink（央企场景慎用，数据会暴露公网）。
- 驾驶舱 UI（v1.5.0）改动：顶部新增"自动同步"状态条（显示最近更新时间 + 各来源模式/错误）；任务与会议室粘贴框新增"上传到本地服务"按钮；"同步读取"改走本地服务端（绕过浏览器 CORS）；启动即尝试从 `/api/data` 拉真实数据覆盖示例，失败则回退示例；每 60 秒自动轮询刷新。
- 经单壳端到端测试：`/api/data`、`/api/upload`(TSV+CSV 均解析)、`/api/sync`(含 drop 回退)、初始无数据状态均通过。
- 版本规则：新增独立同步服务 + UI 自动轮询能力 → 升次版本 v1.5.0。

## v1.4.1 · 2026-08-15
- 修订：用户提供了真实腾讯表格链接（会议室预约登记表 https://docs.qq.com/sheet/DUFNXTnBsV1pGYmZ4?tab=BB08J2）。
- 实测该表为「登录可见」私有表：服务器抓取数据接口返回 401 未授权，页面数据由 JS 动态加载、未内嵌于静态 HTML；浏览器端 fetch 受跨域(CORS) 限制同样读不到。
- 已将真实链接**预填**到"会议室数据"框的公开链接输入框，用户一眼看清指向哪张表。
- 改进"同步读取"失败提示：明确告知失败根因为"跨域拦截 / 表仍为私有登录可见"，并引导改用方式一（复制粘贴 / 上传 CSV）。
- 推断结论：纯静态网页无法免登直连私有腾讯表格，"自动同步"仅在表设为「获得链接的人可阅读（公开）」后才可能成功（仍受 CORS 影响，属最佳努力）。当前最稳方案仍是手动复制粘贴 / 上传 CSV。

## v1.4.0 · 2026-08-15
- **新增：会议室实时预约模块（用户要求，归属"会务接待"板块）。**
- **双数据源落地**：任务数据继续读 WPS 在线表格；新增独立的"会议室数据"入口专门读腾讯在线表格（与任务数据分开导入，互不干扰）。
- 会议室数据接入（与 WPS 同源的免授权方式）：从腾讯表格复制预约区域（含表头）→ 粘贴到"会议室数据"框 → 点"生成会议室"；或上传 CSV；另提供"腾讯表格公开链接 → 同步读取"入口（fetch 最佳努力，跨域受限时自动回退提示改用粘贴/上传）。
- 表头自动识别：会议室/房间、日期、开始时间、结束时间、预约人、用途、状态；支持"起止时间"合并列（如 09:00-10:30）自动拆起止。
- **实时判定**：基于当前时间自动算每间会议室状态（使用中/空闲/即将开始≤30分钟）与每条预约状态（使用中/即将开始/待开始/已结束），页面每 60 秒自动刷新，配"实时刷新中"脉冲指示与当前时间显示。
- 板块内容：会议室总数 / 使用中 / 空闲 / 即将开始 统计卡 + 每间会议室的"今日时间轴"（7:00–22:00 横向排程，含当前时间竖线）+ 今日预约明细列表（带实时状态标签）。
- 该板块随 5 套皮肤一起换色（CSS 变量）。
- 经 node + DOM 桩测试：会议室解析(7)、实时状态/KPI、时间轴 seg/nowmark、CSV、主题切换、任务解析(18)与逾期判定均通过。
- 版本规则：新增独立数据源 + 实时模块 → 升次版本 v1.4.0。

## v1.3.0 · 2026-08-15
- **新增：5 套可切换皮肤（用户要求"都保留、可以更换"）。**
- 文件 `行政部驾驶舱_UI_v1.3.0.html` 在 v1.2.0 全部功能基础上，内置 5 种风格切换器：深色科技大屏 / 浅色商务 / 毛玻璃渐变 / 政务简约 / 暖橙现代。
- 顶部「界面风格」按钮一键换肤；选择通过 localStorage 记住，下次打开沿用。
- 皮肤用 CSS 变量实现，所有卡片/图表/进度环统一随主题刷新（donut、图例、数据条均改为主题变量），无第三方库。
- 完整保留：从 WPS 粘贴 / 上传 CSV 更新数据、全部板块（KPI/完成率环/状态分布/大类/优先级/人员工作台/逾期清单/明细）、逾期自动标红。
- 默认皮肤：深色科技大屏（最像指挥大屏）；用户未选时打开即深色，选过则沿用上次。
- 原 `行政部驾驶舱_风格选择预览.html` 已冗余，本版取代之，删除。
- 版本规则：UI 视觉升级（换肤能力）→ 仍属次版本内增强，升 v1.3.0。

## v1.2.0 · 2026-08-15
- **新增：独立的驾驶舱可视化 UI（与 WPS 后台表分离）。**
- 文件 `行政部驾驶舱_UI_v1.2.0.html`：单文件、纯前端、离线可用、零外部依赖（图表用原生 SVG/CSS 绘制，不引第三方库）。
- 定位：WPS 在线表 = 12 人日常填报的后台数据本；本 HTML = 领导/全员查看的"可视化驾驶舱大屏"，两者解耦。
- 包含板块：KPI 卡片(总任务/进行中/已完成/逾期/完成率环形图)、任务状态分布、各大类任务数、优先级分布、人员工作台(每人任务数+平均进度+逾期)、逾期清单、全部任务明细(进度条)。
- 数据接入方式（免授权、免接口）：在 WPS 全选复制整表 → 粘贴到页面"数据粘贴框" → 点"生成驾驶舱"；或上传 CSV。按表头名称自动识别列，列顺序无关。
- 内置 18 条示例数据（12 名占位同事、覆盖六类与多种状态/逾期），打开即有展示；"恢复示例"可一键还原。
- 视觉：中建蓝(#2E75B6)主色 + 党建红(#C2272D)预警，克制不花哨，浅色专业风。
- 逾期判定逻辑：状态≠已完成 且 截止日<今天 → 自动标红并归入"已逾期"。
- 经 node + DOM 桩测试：解析/逾期判定/CSV/各板块渲染均通过。
- 版本规则：新增独立 UI 模块 → 升次版本 v1.2.0。

## v1.1.0 · 2026-08-15
- **平台迁移：飞书多维表格 → WPS 在线表格（在线 Excel 型）。**
- 迁移原因：用户飞书环境无团队/企业空间，无法把自建应用加为协作者，API 自动建表(91403)走不通；且"移动到"无可用共享空间。用户决定放弃飞书。
- 适配调整（因 WPS 无看板视图/人员字段）：
  - 大类/状态/优先级用「数据验证(下拉)」实现，选项逗号分隔；
  - 进度%用「条件格式-数据条」显示进度条；
  - 逾期标红、状态上色用「条件格式-公式规则」；
  - 是否逾期用 Excel 公式 `=IF(AND(F2<>"已完成",I2<>"",TODAY()>I2),"逾期","正常")`；
  - "每人进展"改用「筛选」按负责人/状态查看；
  - 总览改用独立的「仪表盘」工作表 + COUNTIF 统计 + 插入图表(饼图/柱状图)。
  - 负责人/协同人直接写人名文本（WPS 无人员字段）。
- 权限差异：WPS 在线表只能整表"可看/可编辑"，无飞书式行级/字段级精细权限，靠自觉填自己行。
- 字段清单、六大类、扩展性、进度表现(数字+条)目标不变。
- 配套：WPS 版搭建清单(替换飞书手册)；飞书脚本 build_feishu_cockpit.py / check_fields.py 作废保留。
- **配套交付物 `行政部驾驶舱模板_v1.1.0.xlsx`**：用 openpyxl 生成，含 13 列表头、大类/状态/优先级下拉、是否逾期公式、进度%数据条、逾期整行标红、状态上色、独立「仪表盘」工作表(COUNTIF 统计卡 + 状态饼图 + 各大类柱状图)。导入 WPS 即得完整结构，免去手动点。生成脚本 generate_xlsx.py 一并留存。

## v1.0.0 · 2026-08-15
- 首个定稿版本。
- 确定平台：飞书多维表格。
- 主表「任务总表」字段：任务名称、所属大类(单选)、子项/备注、负责人(人员)、协同人(人员多选)、状态(单选)、进度%、开始日期、截止日期、优先级(单选)、堵点/说明、是否逾期(公式)、更新时间(公式)。
- 六大类：会务接待 / 车辆管理 / 后勤管理 / 物资采购 / 行政办公 / 人力资源，预留"其他"手动加。
- 视图：看板(按负责人)、看板(按状态)、日历、领导视图。
- 仪表盘：总任务/进行中/已完成/逾期 统计卡 + 完成率进度环 + 各大类条形图。
- 角色权限：成员仅编辑自己负责记录；管理员可管理。
- 自动化：逾期自动标红、进度100自动完成。
- 进度表现：数字 + 进度条。
- 扩展性：大类/人员/分工/字段/视图均支持后期增减，按版本规则升版。
- 配套：飞书分步操作手册、GitHub 备份与 Token 获取说明。
